# Tcast Media News Clipping — 프로젝트 노트

이 문서는 이 저장소를 처음 보는 사람(또는 다른 계정/컴퓨터의 Claude)이
"왜 이렇게 만들어져 있는지" 바로 이해할 수 있도록 정리한 배경 설명입니다.
코드 자체의 동작은 `collect_news.ps1`과 `.github/workflows/update-news.yml` 주석에 있고,
여기서는 **설계 이유와 시행착오**를 정리합니다.

## 무엇을 하는 프로젝트인가

대한민국 유료방송(IPTV/SO/PP) 업계 뉴스를 자동으로 수집해서 웹페이지로 보여주는 사이트.
- 공개 URL: https://my-webpage-1-red.vercel.app
- 만든 목적: Tcast 매체영업팀이 팀원들과 함께 볼 업계 뉴스 클리핑

## 전체 구조 (중요: 로컬 PC와 완전히 무관함)

```
GitHub Actions (cron, 6시간마다 — KST 00시/06시/12시/18시. 2026-08-28에 3시간→6시간으로 변경)
  → collect_news.ps1 실행 (pwsh, ubuntu-latest 러너)
  → index.html + news_history.json + collect_log.txt 생성
  → GITHUB_TOKEN으로 자동 git commit + push
  → Vercel이 push 감지, 자동 재배포
```

**처음에는 로컬 PC의 Windows 작업 스케줄러로 돌렸는데, "PC를 꺼도 사이트가 갱신되어야
팀원 공유가 의미 있다"는 이유로 GitHub Actions 클라우드 실행으로 완전히 옮겼습니다.**
그래서 지금은 로컬 PC가 켜져 있든 꺼져 있든, 클로드와의 대화가 끊기든 전혀 상관없이
독립적으로 계속 돌아갑니다.

## 뉴스 수집 방식

- **RSS**: 전자신문, 미디어스, 미디어오늘 (안정적, 표준 RSS)
- **네이버 뉴스 검색 스크래핑**: 정식 API가 아니라 검색결과 HTML을 정규식으로 파싱.
  네이버가 프론트엔드 구조(`sds-comps-*` 클래스명 등)를 바꾸면 깨질 수 있음 — 그래도
  RSS 쪽은 살아있어서 사이트 자체가 죽지는 않고, "새 네이버 기사만" 안 들어옴.
  → 스크래핑이 안 되는 것 같으면 `collect_log.txt`의 "네이버 검색 실패" 로그부터 확인.

## 키워드 필터링 — 3단계 신뢰도 구조 (여러 번 튜닝한 결과물)

1. **`$CoreIndustryKeywords`** — IPTV/SO/유료방송 핵심 용어. 무조건 신뢰, 예외 없음.
   → "핵심 유료방송" 탭의 기준.
2. **`$ExtendedTopicKeywords`** — OTT 서비스명, 국내 유료방송사·채널사 고유명사
   (SK브로드밴드, MBC, JTBC 등). 구체적 고유명사라 오탐 위험 낮아서 무조건 인정.
3. **`$BroadKeywords`** (콘텐츠/미디어/OTT/프로그램 등 범용 단어) +
   **`$HighVolumeEntityKeywords`** (삼성/애플/KT/SK텔레콤) —
   이 두 그룹은 **단독으로는 절대 인정 안 함**. 이유:
   - `콘텐츠`, `프로그램` 같은 범용 단어를 그냥 허용하면 건설사 마케팅, IT 보안 뉴스,
     기업 소프트웨어 뉴스까지 다 섞여 들어옴 → `$ExclusionKeywords`로 걸러냄
     (건설/부동산/해킹/개인정보/야구단 등)
   - `KT`, `삼성`, `애플`처럼 사업 영역이 넓은 대기업명은 `$MediaContextWords`
     (방송/채널/미디어/콘텐츠 등)와 **같이 나올 때만** 인정. 안 그러면 "KT 클라우드 데이터센터",
     "kt wiz 야구단" 같은 완전 무관한 뉴스가 쏟아짐.
   - `KT`, `SO`는 영문 약자라 다른 단어에 우연히 낀 경우(TikTok, SOC 등)를 걸러내려고
     정규식 단어 경계(`(?<![A-Za-z0-9])KT(?![A-Za-z0-9])`)로 따로 처리.
   - `가처분`,`인수합병`,`M&A`,`유튜브`,`크리에이터`는 `$ContextGatedKeywords`로 따로 관리
     (미디어 맥락 단어와 같이 나올 때만 인정 — "가처분"은 부동산/노동 분쟁에도 흔한 단어라
     단독 허용하면 무관 기사가 쏟아짐).

**주의: 어떤 단어를 "무조건 신뢰"(1·2단계) 목록에 넣을지는 신중하게 판단할 것.**
2026-08-26에 `홈쇼핑`/`T커머스`를 2단계(무조건 인정)에 넣었다가, 화장품 브랜드
"도미나스"의 "홈쇼핑 누적 매출 2000억" 기사, 농심의 "신라면 콘텐츠 사업" 기사처럼
업계와 무관한 소비재 기업 뉴스가 새는 문제를 겪었음 → 3단계(제외어 필터 적용)로
이동하고 `$ExclusionKeywords`에 `화장품`,`뷰티`,`생활건강`,`라면`,`식품`,`제약` 추가로 해결.
**"국내 유료방송사·채널사 고유명사"가 아니라 일반 업종 용어(홈쇼핑, 콘텐츠 등)라면
처음부터 3단계에 넣는 게 안전함.**

**`콘텐츠`/`채널`은 특히 위험한 단어임 — 계속 새로운 오탐이 나옴.** "채널"은 방송
채널 말고도 "디지털 채널"(유통), "유튜브 채널"(브랜드 홍보), "판매 채널" 등 완전히
다른 뜻으로 훨씬 더 자주 쓰임. "콘텐츠"도 마찬가지로 아무 브랜드나 "K-헤리티지
콘텐츠", "IP 콘텐츠 사업" 같은 홍보 문구에 갖다 씀. 실제로 겪은 오탐 사례
(전부 `$ExclusionKeywords`에 브랜드명을 직접 추가해서 해결, 2026-08-28):
- 삼성전자 "디지털고객경험지수(DCXI)" 가전 순위 기사 — "디지털 **채널**"
- 쿠키런 게임 매출 기사 — "공식 유튜브 **채널**과 네이버 카페"
- 정관장 신제품 기사 — "K-헤리티지 **콘텐츠**"
- 도미나스(화장품), 농심(식품) — 위에서 설명한 사례

**패턴을 알아둘 것**: 이런 식으로 특정 브랜드가 "채널"/"콘텐츠" 문구가 들어간
보도자료를 반복해서 뿌리는 경우가 계속 나올 수 있음. 사용자가 무관한 기사를
신고하면 (1) 왜 걸렸는지(어느 키워드) 확인 → (2) 브랜드명을 `$ExclusionKeywords`에
직접 추가하는 게 제일 빠르고 안전한 해결책(다른 매칭에 영향 없음). 재발하면
같은 방식으로 계속 추가하면 됨.

또 하나 실제로 겪은 버그: 네이버 검색 결과 매칭이 **기사 제목만** 검사하고 있어서,
제외어가 요약(스니펫)에만 있는 경우(도미나스 기사가 정확히 이 케이스) 걸러지지 않고
새는 문제가 있었음. RSS 수집은 처음부터 "제목+요약"을 합쳐서 검사하고 있었는데
네이버 쪽만 빠져 있었던 것 → 두 경로(신규 수집 시 `Test-TopicMatch`, 히스토리 재검증 시
`Load-History` 내부) 모두 제목+요약을 합쳐서 검사하도록 통일함(2026-08-28).
**앞으로 새 수집 경로를 추가하면 반드시 제목+요약을 합쳐서 `Test-TopicMatch`에
넘기고 있는지 확인할 것.**

## PP(방송채널사용사업자) 탭

MBC/KBS/SBS/JTBC/tvN/CJ ENM 같은 채널·콘텐츠 공급사 관점 뉴스를 따로 모으는 탭.
SO/IPTV(플랫폼 쪽)와는 다른 축이라 "핵심 유료방송" 탭과 분리했고, 신작 드라마·예능
편성/제작발표회 같은 콘텐츠 제작 소식도 포함되도록 키워드를 넣어둠.

## 유사기사(중복보도) 묶음 처리

실적 발표 하나를 10개 매체가 거의 같은 제목으로 보도하는 경우가 많아서, 제목 단어
겹침(자카드 유사도) + 사업자명 일치를 기준으로 묶어서 **묶음당 최대 5건**만 노출.
그래도 남는 자리는 다른 주제 기사로 채워짐 (단, 그날 다른 화제가 없으면 그냥 적게 나옴 —
버그 아니라 실제로 뉴스가 없는 것).

## 뉴스 누적(히스토리) 구조 — 중요한 버그를 한 번 겪고 고침

처음엔 매 실행이 그 순간의 네이버 실시간 검색 결과만 스냅샷으로 저장했는데, 이러면
한동안 안 열어본 사이에 화제였다가 지나간 뉴스가 다음 실행에서 통째로 사라지는 문제가 있었음.
→ `news_history.json`에 이전 결과를 계속 누적 저장하고, 매 실행마다 새로 수집한 것과
합쳐서 최근 7일치를 유지하도록 변경.

**저장 공간은 카테고리별로 분리되어 있음** (핵심/PP/그 외 각각 독립 예산 200~300건).
이유: 물량이 많은 카테고리(예: 미디어·콘텐츠 전체)가 저장공간을 다 차지해버리면,
물량 적은 카테고리(핵심 유료방송)가 7일을 다 채우기도 전에 밀려나는 문제를 실제로
겪었음. 앞으로 새 카테고리 탭을 추가한다면 이 저장공간 분리 패턴을 그대로 따라야 함.

**"🆕 최신 뉴스" 탭만 4일 컷오프(2026-08-28 변경)**: 위 7일 누적은 "핵심 유료방송"/"PP 소식"
탭 기준이고, 맨 앞 "최신 뉴스" 탭은 사용자 피드백으로 4일로 좁혔음 (일주일 전 기사까지
섞여 있어서 "최신"이라는 이름과 안 맞는다는 지적). `$LatestTabRetentionHours`가 그 값이고,
`$final`(최신 뉴스 탭)에만 적용되며 `$coreFinal`/`$ppFinal`은 그대로 7일 유지.

## 시간대 처리 주의사항

GitHub Actions 러너의 시스템 시각은 UTC. 한국 독자 기준 시각이 나오도록
`(Get-Date).ToUniversalTime().AddHours(9)`로 고정 오프셋 보정함 (한국은 서머타임 없어서
안전). 요일 표시(`ddd`)도 러너 기본 로케일이 en-US라 "Thu"로 나오는 문제가 있어서
`ko-KR` CultureInfo를 명시적으로 지정해서 "목"이 나오도록 고침.

## Git 인증 관련

로컬에서 수동으로 push할 때는 `GCM_INTERACTIVE=never` 환경변수를 꼭 붙여야 함
(Windows Git Credential Manager가 캐시된 인증정보를 씀 — 예약작업처럼 아무도 안 보는
상황에서 로그인 창을 띄우려다 무한 대기하는 것을 방지). GitHub Actions 내부에서는
`GITHUB_TOKEN`을 자동으로 쓰기 때문에 이 문제 자체가 없음.

로컬에서 수정하고 push할 때는 원격에 이미 새 자동커밋(`뉴스 자동 갱신 ...`)이 올라와
있는 경우가 많아서, 그냥 `git pull --rebase`하면 index.html 등 생성 파일에서 충돌이 남.
→ 이럴 땐 수정한 스크립트 파일만 임시 백업 → `git reset --hard origin/main`으로 원격과
동기화 → 백업한 파일 다시 덮어쓰기 → commit/push. (이 저장소에서 반복적으로 쓴 패턴)

## 예약 실행(cron)이 갑자기 멈췄을 때 — 실제로 겪은 사례 (2026-08-26~28)

`뉴스 자동 갱신` 커밋이 08-26 14:24 UTC를 마지막으로 약 34시간 동안 하나도 안 올라온 적이 있었음.
워크플로는 "active" 상태였고 저장소도 정상(public/미보관)이었는데, **예약(schedule) 이벤트
자체가 트리거되지 않는** 상황이었음 — 실패 로그조차 없이 실행 자체가 안 생김.

**진단 방법** (로그인 없이도 가능, 공개 API):
```
curl -s "https://api.github.com/repos/imtcast8145-lgtm/my-webpage-1/actions/runs?per_page=20"
```
`workflow_runs[].event`가 전부 `schedule`인데 최근 것만 안 보이면 트리거가 멈춘 것.
`https://www.githubstatus.com/api/v2/incidents.json`도 같이 확인 — 이때는 실제로 GitHub
자체의 **"Disruption with GitHub Billing"** 전역 장애(08-26 23:37 ~ 08-27 19:44 UTC)와
겹쳤음. Actions 예약 실행은 내부적으로 결제/쿼터 확인을 거치는데 그게 막히면 스케줄
트리거 자체가 조용히 안 도는 것으로 추정.

**해결(임시 조치)**: 이 워크플로는 `on: push: branches: [main]`도 트리거로 걸려 있어서,
빈 커밋을 하나 push하면(`git commit --allow-empty`) 즉시 강제 실행됨. 장애가 GitHub 쪽에서
풀린 뒤에도 예약 트리거는 스스로 안 깨어날 수 있어서, 이렇게 한 번 "깨워주는" 게 효과적이었음.
이후 실제로 `event: schedule`인 실행이 다시 스스로 도는 것까지 확인하고 나서야 완전히
정상화됐다고 판단할 수 있음(단순히 워크플로 파일 "active" 상태만으로는 확인 안 됨).

**주의: 완전히 안정화된 게 확인 안 됐음(2026-08-28 기준 진행 중).** 08-28 01:00 UTC에
schedule 이벤트가 한 번 스스로 돌았지만, 그 다음 두 슬롯(03:00, 06:00 UTC)은 또
건너뛰었음. 저장소 Settings → Actions → General → "Actions permissions"는
"Allow all actions and reusable workflows"로 정상 확인함 — 저장소 설정 문제는 아님.
GitHub 쪽 스케줄 백로그가 아직 덜 풀렸을 가능성이 유력. 이어서 작업하는 사람은
```
curl -s "https://api.github.com/repos/imtcast8145-lgtm/my-webpage-1/actions/runs?per_page=10"
```
로 최근 `event: schedule` 실행이 계속 나오는지(=정상화) 확인부터 할 것. 계속 안 돌면
빈 커밋 push로 계속 수동 보충하거나, 외부 무료 크론 서비스(cron-job.org 등)로 주기적으로
`workflow_dispatch`를 호출해주는 보조 장치를 고려.

**2026-08-28에 갱신 주기를 3시간 → 6시간(KST 0/6/12/18시)으로 변경**함(사용자 요청).
`.github/workflows/update-news.yml`의 cron 표현식은 `'0 3,9,15,21 * * *'`(UTC).

## 아직 안 한 것 / 향후 고려사항

- 로컬 PC의 `C:\Users\User\Desktop\my-webpage\collect_news.ps1`은 GitHub Actions로
  옮기기 전 버전이라 이 저장소 버전과 갈라져 있음. 필요 없으면 정리해도 됨.
- Naver 스크래핑은 언제든 깨질 수 있는 약한 지점 — 정기적으로 `collect_log.txt` 확인 권장.
- 저장소가 비공개(private)로 바뀌면 GitHub Actions 무료 사용량 제한이 생기니, 그때는
  cron 주기를 다시 조정해야 할 수 있음.
